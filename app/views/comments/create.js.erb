//when user adds new comment, comment box hides and displays message
$("#new_comment").html("Thank you for your review!");
//comments ordered to show most recent one at the top with prepend
$('.product-reviews').prepend("<%= j render @comment %>");
//update display of average rating
$("#average-rating").attr('data-score', "<%= @product.average_rating %>");
refreshRating();
